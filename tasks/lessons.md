# Lessons

- `.photoslibrary` は通常ファイルではなくパッケージフォルダーとして扱われる。選択 UI では `choose file` より `choose folder` ベースで扱う方が安全。
- `.app` バンドル内部を直接更新した場合は、コード署名の整合性も確認する。必要なら配布用スクリプトにアドホック再署名を組み込む。
- `.app` に対して `chmod -R 755` をかけると plist やリソースまで実行ファイル扱いになり、署名検証を壊す。ディレクトリ・通常ファイル・`Contents/MacOS` を分けて権限を整える。
